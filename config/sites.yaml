# 감시할 사이트 목록
#
# 각 항목:
#   name:     사이트 구분용 이름 (영문/숫자/하이픈, 파일명에 사용됨)
#   url:      가져올 주소
#   selector: (선택) 페이지에서 이 CSS 선택자 부분만 감시. 생략하면 본문 전체.
#   headers:  (선택) 요청에 추가할 HTTP 헤더 (User-Agent, Cookie 등)

sites:
  - name: example
    url: https://example.com
    selector: "h1"
